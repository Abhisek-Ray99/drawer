import { StyleSheet, Text, View } from 'react-native'
import React, {memo} from 'react'

const ProfileScreenEdit = () => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default memo(ProfileScreenEdit)

const styles = StyleSheet.create({})